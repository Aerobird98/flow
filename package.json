{
  "name": "flow",
  "version": "0.12.1",
  "description": "A compact web based rich-text editor that builds on top of Slate-js with powerful, yet simple and accessible editing capabilities.",
  "private": true,
  "scripts": {
    "build-development": "esbuild index.js --bundle --outdir=build --loader:.js=jsx --jsx-factory=h --jsx-fragment=Fragment --minify --sourcemap --define:process.env.NODE_ENV='\"development\"'",
    "build-production": "esbuild index.js --bundle --outdir=build --loader:.js=jsx --jsx-factory=h --jsx-fragment=Fragment --minify --sourcemap --define:process.env.NODE_ENV='\"production\"'",
    "deploy": "gh-pages -d build",
    "release": "npm run build-production && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aerobird98/flow.git"
  },
  "author": "Aerobird98",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Aerobird98/flow/issues"
  },
  "homepage": "http://Aerobird98.github.io/flow",
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^1.2.31",
    "@fortawesome/free-solid-svg-icons": "^5.15.0",
    "@fortawesome/react-fontawesome": "^0.1.11",
    "preact": "^10.5.3",
    "slate": "^0.58.4",
    "slate-history": "^0.58.4",
    "slate-react": "^0.58.4",
    "theme-ui": "^0.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.5.26",
    "gh-pages": "^3.1.0",
    "prettier": "^2.1.2"
  }
}
